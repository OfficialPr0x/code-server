// Export your contract ABIs here
const contractABIs = {
    // Add your contract ABIs here
};

if (typeof module !== 'undefined') {
    module.exports = contractABIs;
} else {
    window.contractABIs = contractABIs;
}
